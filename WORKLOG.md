# Worklog

## 2026-03-15
- Established project documentation workflow.
- Added `AGENTS.md` for stable rules and startup routine.
- Added `PROJECT_STATE.md` for current working context between chats.
- Added `WORKLOG.md` for short chronological notes.
- Verified TouchDesigner MCP connectivity and inspected `/project1`.
- Added the rule that important DEV documentation should also be mirrored inside TouchDesigner as synced `textDAT` nodes.
- Added the rule that support/documentation nodes in TouchDesigner should be grouped and arranged logically.
- Mirrored project documentation into TouchDesigner under `/project1/project_docs` as synced `textDAT` nodes.
- Initialized Git in the project root.
- Added ignore rules for non-essential TouchDesigner information and backups.
- Connected the local repository to GitHub at `soulexx/SLS_Studio`.
- Created and pushed the initial baseline commit on `main`.
- Added the rule to ask about documentation updates when relevant project parts are added, removed, replaced, or reorganized.
- Added the standard TouchDesigner device layout rule: `I/O -> Verarbeitung -> Bus -> Debug -> Tests`.
- Reordered `vcm600` to follow the standard device layout.
- Reordered `cntrlr`, `midicon`, and `midicraft` to follow the standard device layout.
- Renamed `_Touchdesigner/devices/MAPPING_RULES.md` to `_Touchdesigner/devices/DEVICE_RULES.md`.
- Updated `AGENTS.md` so device work explicitly reads `DEVICE_RULES.md` as the authoritative mapping reference.
- Renamed the TouchDesigner sync node from `/project1/devices/mapping_rules` to `/project1/devices/device_rules`.
- Changed the rule so documentation and rules files in DEV are always mirrored to TouchDesigner as synced `textDAT` nodes.
- Added `_Touchdesigner/bitwig/BITWIG_RULES.md` as the Bitwig-specific rule file.
- Mirrored `BITWIG_RULES.md` into TouchDesigner as `/project1/bitwig/bitwig_rules`.
- Added the layout rule that rule `textDAT` blocks should always appear at the beginning of a logical TouchDesigner area.
- Repositioned `device_rules` and `bitwig_rules` so they appear first in their respective areas.
- Verified TouchDesigner and Bitwig are both running again with the documented OSC ports open.
- Started restructuring the project toward hybrid shared `channel_state` and output consumers.
- Separated the VCM-600 production LED transport from the debug-only LED test API.
- Added explicit slot focus so the last selected `track_1.1` to `track_6.3` drives the Bitwig track selection.
- Replaced frame-end polling in channel-selection outputs with DAT-change-driven triggers.
